#pragma once

/**
 * @file HarpDsp.h
 * Compatibility include for firmware that wants the engine by class name.
 *
 * Include engine.h directly when you want the narrower engine API header.
 */

#include "engine.h"
